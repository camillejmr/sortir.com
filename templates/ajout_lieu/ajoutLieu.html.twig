{% extends 'base.html.twig' %}

{% block title %}
    Ajout d'un lieu | {{ parent() }}
{% endblock %}

{% block body %}
    <h1>Ajout d'un lieu</h1>
    <div class="formulaire">
        {{ form_start(AjoutLieuForm) }}
        {{ form_widget(AjoutLieuForm) }}
        <button type="submit" class="btn">Ajouter un lieu</button>
        {{ form_end(AjoutLieuForm) }}
        <a href="{{ path('creer_Sortie', {'idOrganisateur':app.user.id}) }}" class="btn">
         Annuler
        </a>
    </div>


{% endblock %}




