{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | S'inscirre à une sortie{% endblock %}

{% block body %}
    <form method="post">{{ form_start(form) }}
        <div id="inscriptionSortie">
            {{ app.user.prenom }}, êtes-vous sûr(e) de vouloir supprimer la sortie {{ sortie.nom }} ?
        </div>


        <button type="submit" id="insciptionSortie">Supprimer la sortie</button>
        <a href="{{ path('main_home') }}">{{ form_end(form) }}
            <button type="action" id="annuler"> Annuler</button>
        </a>
    </form>
{% endblock %}



