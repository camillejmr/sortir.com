{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Accueil{% endblock %}

{% block body %}
    <div id="infos">
        <div>Date du jour : {{ "now" | date("d/m/Y") }}</div>
        <div>Participant : Josiane K.</div> {# A TESTER : {{ app.user.prenom }} {{ app.user.nom|slice(0, 1) }}. #}
    </div>
    <div id="rechercher_sorties">
        <h2>Filtrer les sorties</h2>
        {# FORMULAIRE DE RECHERCHE (en inclusion pour + de propreté) #}
        {% include 'inc/rechercheSorties.html.twig' %}

        {# RESULTATS RECHERCHE #}
        {% for sortie in sorties %}
        {% endfor %}
    </div>
{% endblock %}



