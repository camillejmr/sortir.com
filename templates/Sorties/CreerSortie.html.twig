{% extends 'base.html.twig' %}

{% block title %}
    Creation d'une sortie | {{ parent() }}
{% endblock %}

{% block body %}
    <style>
        #plus {position:absolute; right:32%; height:30px; width:30px; margin-top:-60px;}
%;
    </style>
    <h1>Créer une sortie</h1>
    <div class="formulaire">
        {{ form_start(CreerSortieForm) }}
        {{ form_widget(CreerSortieForm) }}
        <div id="logo_+">
            <a href="{{ path('ajout_lieu', {'idOrganisateur':1}) }}"><img src="{{ asset('img/plus.png') }}" alt="ajouter_lieu" id="plus"></a>
        </div>
        <button type="submit" class="btn">Créer une sortie</button>
        {{ form_end(CreerSortieForm) }}
        <a href="{{ path('main_home') }}">
            <button  class="twelve" id="annuler"> Annuler</button>
        </a>
    </div>


{% endblock %}




